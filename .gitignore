# ความลับ — ห้ามขึ้น git (repo นี้เป็น public)
ngrok-token.txt

# ไบนารีที่สคริปต์โหลดมาเองตอนรัน
ngrok.exe
cloudflared.exe
ngrok.zip

__pycache__/
*.pyc
_exports/

# ไฟล์ backup ของ MuMu — mumu-backup = ฝั่ง server (ไฟล์ต้นฉบับที่เอาไปวางเอง)
# mumu-cache = ฝั่ง agent (ไฟล์ที่โหลดมาเก็บไว้) + สถานะงาน .bat ที่สั่งรัน
mumu-backup/
mumu-cache/
_run_state.json

# master tunnel (pc_1) - secret + runtime files, never commit
github-token.txt
cloudflare-token.txt
tunnel-url.txt
tunnel-fixed-url.txt
master-tunnel.log
cloudflared.log
cloudflared.out.log
*.bak
