# Remote File Manager - Dependencies
# ติดตั้ง: pip install -r requirements.txt

# === สำหรับเครื่องหลัก (Server) ===
flask==3.1.1
flask-socketio==5.5.1

# === สำหรับเครื่องลูก (Agent) ===
python-socketio[client]==5.12.1
websocket-client==1.8.0
